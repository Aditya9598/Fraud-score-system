[package]
name = "scorer"
version = "0.1.0"
edition = "2021"

[lib]
name = "scorer"
path = "src/lib.rs"

[[bin]]
name = "scorer"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
